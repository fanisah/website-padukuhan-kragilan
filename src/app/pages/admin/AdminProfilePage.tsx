import { useEffect, useState, type FormEvent, type ReactNode } from "react";
import { getProfileForAdmin, updateProfile } from "../../../services/adminProfile";
const inputClass="h-12 w-full rounded-xl border border-[#C8D5D0] bg-white px-4 text-sm outline-none focus:border-[#0D6F6B] focus:ring-2 focus:ring-[#0D6F6B]/15";
export default function AdminProfilePage(){const[form,setForm]=useState({sejarah:"",visi:"",misi:""});const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[message,setMessage]=useState("");const[error,setError]=useState("");useEffect(()=>{let active=true;getProfileForAdmin().then(p=>active&&setForm({sejarah:p.sejarah??"",visi:p.visi??"",misi:p.misi??""})).catch(()=>active&&setError("Profil belum dapat dimuat.")).finally(()=>active&&setLoading(false));return()=>{active=false}},[]);async function submit(e:FormEvent){e.preventDefault();setSaving(true);setError("");setMessage("");try{await updateProfile({sejarah:form.sejarah.trim()||null,visi:form.visi.trim()||null,misi:form.misi.trim()||null});setMessage("Profil berhasil diperbarui.")}catch{setError("Profil tidak dapat disimpan. Silakan coba kembali.")}finally{setSaving(false)}}if(loading)return <p className="rounded-2xl border bg-white p-10 text-center text-sm">Memuat Profil…</p>;return <section className="max-w-3xl"><h1 className="text-[1.75rem] font-bold">Profil</h1><p className="mt-2 text-sm text-[#5F6F72]">Informasi di halaman ini akan tampil pada menu Profil.</p>{message&&<p className="mt-6 rounded-xl bg-emerald-50 p-4 text-sm text-emerald-700">{message}</p>}{error&&<p className="mt-6 rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</p>}<form onSubmit={submit} className="mt-6 space-y-5 rounded-2xl border border-[#D8E4DF] bg-white p-5 sm:p-7"><Field label="Sejarah Padukuhan" help="Ceritakan asal-usul dan perkembangan Padukuhan Kragilan."><textarea className={`${inputClass} min-h-40 py-3`} value={form.sejarah} onChange={e=>setForm({...form,sejarah:e.target.value})}/></Field><Field label="Visi" help="Tuliskan tujuan atau gambaran masa depan Padukuhan."><textarea className={`${inputClass} min-h-28 py-3`} value={form.visi} onChange={e=>setForm({...form,visi:e.target.value})}/></Field><Field label="Misi" help="Tuliskan langkah-langkah untuk mewujudkan visi."><textarea className={`${inputClass} min-h-40 py-3`} value={form.misi} onChange={e=>setForm({...form,misi:e.target.value})}/></Field><button disabled={saving} className="min-h-12 w-full rounded-xl bg-[#0D6F6B] font-bold text-white disabled:opacity-60">{saving?"Menyimpan…":"Simpan Perubahan"}</button></form></section>}
function Field({label,help,children}:{label:string;help:string;children:ReactNode}){return <label className="block"><span className="block text-sm font-semibold">{label}</span><span className="mb-2 mt-1 block text-xs text-[#5F6F72]">{help}</span>{children}</label>}
